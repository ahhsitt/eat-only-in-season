# Design Model: 活泼年轻风沉浸式 UI 重设计

**Feature**: 005-page-ui-redesign
**Date**: 2026-01-13

## 概述

本文档定义 UI 重设计的设计系统、组件模型和视觉规范。由于本功能为纯前端 UI 变更，不涉及数据模型或 API 契约变更。

---

## 1. 设计系统 (Design Tokens)

### 1.1 色彩系统

```typescript
// theme/tokens/colors.ts
export const colors = {
  // 主色调 - 珊瑚橙粉系
  primary: {
    50: '#FFF5F5',
    100: '#FFE4E4',
    200: '#FFB8B8',
    300: '#FF8A8A',
    400: '#FF6B6B',  // 主色
    500: '#FA5252',
    600: '#E03131',
    700: '#C92A2A',
    800: '#A61E1E',
    900: '#801515',
  },

  // 次要色 - 温暖橙粉
  secondary: {
    400: '#FFA07A',  // 主次要色
    500: '#FF8C5A',
  },

  // 强调色 - 明亮柠檬黄
  accent: {
    400: '#FFE66D',  // 主强调色
    500: '#FFD93D',
  },

  // 功能色
  success: '#4ECDC4',
  warning: '#FFB347',
  error: '#FF6B6B',
  info: '#74B9FF',

  // 中性色
  neutral: {
    50: '#FFFAF0',   // 暖白背景
    100: '#FFF5F5',  // 淡粉背景
    200: '#F8F9FA',
    300: '#E9ECEF',
    400: '#DEE2E6',
    500: '#ADB5BD',
    600: '#6C757D',
    700: '#495057',
    800: '#343A40',
    900: '#2D3436',  // 深灰文字
  },
};
```

### 1.2 间距系统

```typescript
// theme/tokens/spacing.ts
export const spacing = {
  xs: '4px',
  sm: '8px',
  md: '16px',
  lg: '24px',
  xl: '32px',
  '2xl': '48px',
  '3xl': '64px',
  '4xl': '96px',
};
```

### 1.3 圆角系统

```typescript
// theme/tokens/radius.ts
export const radius = {
  sm: '8px',
  md: '12px',
  lg: '16px',
  xl: '24px',
  full: '9999px',
};
```

### 1.4 动画系统

```typescript
// theme/tokens/motion.ts
export const motion = {
  // 缓动函数
  easing: {
    default: 'cubic-bezier(0.4, 0, 0.2, 1)',
    easeIn: 'cubic-bezier(0.4, 0, 1, 1)',
    easeOut: 'cubic-bezier(0, 0, 0.2, 1)',
    bounce: 'cubic-bezier(0.68, -0.55, 0.265, 1.55)',
  },

  // 持续时间
  duration: {
    fast: '150ms',
    normal: '300ms',
    slow: '500ms',
  },
};
```

---

## 2. 组件设计模型

### 2.1 页面组件层级

```text
App
├── Layout (全局布局)
│   ├── Header (顶部导航)
│   ├── Content (内容区)
│   └── Footer (底部)
│
├── NewHome (沉浸式首页)
│   ├── HeroSection (全屏欢迎区)
│   │   ├── AnimatedBackground (动态背景)
│   │   └── SearchBox (搜索框)
│   ├── PreferenceCard (偏好设置卡片)
│   ├── LocationBadge (位置信息)
│   ├── IngredientList (食材列表)
│   │   └── IngredientCard (食材卡片) [多个]
│   └── ActionButton (获取菜谱按钮)
│
├── RecipeListPage (沉浸式菜谱列表)
│   ├── PageHeader (页面标题)
│   └── RecipeGrid (菜谱网格)
│       └── RecipeCard (菜谱卡片) [多个]
│
├── NewRecipeDetailPage (杂志风格详情页)
│   ├── HeroImage (大图区)
│   ├── RecipeInfo (菜谱信息)
│   ├── IngredientsSection (食材区)
│   ├── StepsSection (步骤区)
│   └── ExportButton (导出按钮)
│
└── IngredientModal (食材详情弹窗)
    ├── ModalHeader (弹窗标题)
    ├── IngredientInfo (食材信息)
    └── ModalFooter (弹窗底部)
```

### 2.2 组件状态模型

#### HeroSection 状态
```typescript
interface HeroSectionState {
  isSearching: boolean;      // 搜索中动画
  hasSearched: boolean;      // 已搜索，显示结果区
  animationEnabled: boolean; // 动画是否启用
}
```

#### IngredientCard 状态
```typescript
interface IngredientCardState {
  isSelected: boolean;  // 选中状态
  isHovered: boolean;   // 悬停状态
  isAnimating: boolean; // 动画播放中
}
```

#### RecipeCard 状态
```typescript
interface RecipeCardState {
  isHovered: boolean;   // 悬停放大
  isLoading: boolean;   // 图片加载中
  imageLoaded: boolean; // 图片已加载
}
```

---

## 3. 布局规范

### 3.1 首页沉浸式布局

```text
┌─────────────────────────────────────────┐
│                                         │
│         全屏欢迎区 (100vh)              │
│    ┌─────────────────────────────┐      │
│    │      应用标题 + 标语        │      │
│    │                             │      │
│    │    [ 城市搜索框 🔍 ]       │      │
│    │                             │      │
│    │      口味偏好（可选）       │      │
│    └─────────────────────────────┘      │
│                                         │
│             ↓ 滚动指示                  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         食材选择区 (动态高度)           │
│    ┌─────────────────────────────┐      │
│    │  📍 杭州 · 冬季 · 1月      │      │
│    └─────────────────────────────┘      │
│                                         │
│    ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐      │
│    │蔬 │ │菜 │ │鱼 │ │肉 │ │果 │      │
│    │菜 │ │叶 │ │类 │ │类 │ │蔬 │      │
│    └───┘ └───┘ └───┘ └───┘ └───┘      │
│                                         │
│        [  获取菜谱推荐 🍳  ]            │
└─────────────────────────────────────────┘
```

### 3.2 菜谱列表沉浸式布局

```text
┌─────────────────────────────────────────┐
│  ← 返回            菜谱推荐        ···  │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │                                 │   │
│  │         大图菜谱卡片 1          │   │
│  │                                 │   │
│  │    菜名                        │   │
│  │    描述...                      │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │                                 │   │
│  │         大图菜谱卡片 2          │   │
│  │                                 │   │
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

### 3.3 响应式断点

| 断点 | 宽度 | 首页列数 | 菜谱列数 | 导航模式 |
|------|------|---------|---------|---------|
| Mobile | < 768px | 2 | 1 | 底部 |
| Tablet | 768-1024px | 3 | 2 | 顶部简化 |
| Desktop | > 1024px | 4 | 2-3 | 顶部完整 |

---

## 4. 动画规范

### 4.1 入场动画

| 元素 | 动画类型 | 时长 | 缓动 |
|------|---------|------|------|
| 页面 | fadeIn | 300ms | easeOut |
| 卡片 | slideUp + fadeIn | 400ms | bounce |
| 弹窗 | scale + fadeIn | 300ms | bounce |
| 按钮 | scale | 150ms | easeOut |

### 4.2 交互动画

| 交互 | 动画效果 | 时长 |
|------|---------|------|
| 卡片悬停 | scale(1.02) + shadow | 200ms |
| 按钮悬停 | brightness(1.1) | 150ms |
| 选中食材 | scale(0.95) → scale(1) + border | 300ms |
| 点击反馈 | scale(0.98) | 100ms |

### 4.3 降级策略

```css
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 5. 文件变更清单

### 5.1 需修改的文件

| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| `src/theme/index.ts` | 重写 | 新色彩系统 |
| `src/components/Layout/Layout.tsx` | 重构 | 新导航设计 |
| `src/pages/NewHome/NewHome.tsx` | 重构 | 沉浸式布局 |
| `src/components/CityInput/CityInput.tsx` | 增强 | 添加动画 |
| `src/components/IngredientList/IngredientList.tsx` | 重构 | 活力卡片 |
| `src/components/IngredientModal/IngredientModal.tsx` | 增强 | 弹窗动画 |
| `src/pages/RecipeListPage/RecipeListPage.tsx` | 重构 | 沉浸式列表 |
| `src/components/RecipeCard/RecipeCard.tsx` | 重构 | 大图卡片 |
| `src/pages/NewRecipeDetail/NewRecipeDetailPage.tsx` | 重构 | 杂志风格 |

### 5.2 新增文件

| 文件路径 | 说明 |
|---------|------|
| `src/theme/tokens/colors.ts` | 色彩 token |
| `src/theme/tokens/motion.ts` | 动画 token |
| `src/styles/animations.css` | 全局动画定义 |
| `src/hooks/useReducedMotion.ts` | 动画降级 hook |

---

## 6. 数据模型

**无变更** - 本功能为纯 UI 重构，不涉及数据模型变更。现有的 TypeScript 类型定义保持不变：

- `Location`
- `SeasonalIngredient`
- `IngredientCategoryGroup`
- `Recipe`
- `NewUserPreference`

---

## 7. API 契约

**无变更** - 本功能不涉及 API 变更。现有接口保持不变：

- `GET /api/ingredients` - 获取应季食材
- `GET /api/recipes` - 获取菜谱推荐
- `GET /api/recipe/:id` - 获取菜谱详情
